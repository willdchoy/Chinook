global:
  scrape_interval: 15s
  external_labels:
    monitor: 'ch-obsvr'

scrape_configs:
  - job_name: 'ch-prometheus'
    scrape_interval: 5s
    static_configs:
      - targets: ['localhost:9090']
