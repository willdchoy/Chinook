name: ch
services:
  api-web:
    build:
      context: .
    environment:
      DEVELOPMENT: true
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw # why do I have to have this here to pass the docker build?
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/host/rootfs:ro
