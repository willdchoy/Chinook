name: ch
services:
  db-pg:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: chinook
    volumes:
      - ./pg-data:/var/lib/postgresql
    env_file:
      - ./.env
    healthcheck:
      test: ['CMD-SHELL', "sh -c 'pg_isready -U ${DB_USER} -d ${DB}'"]
      interval: 3s
      timeout: 3s
      retries: 30

volumes:
  pg-data:
