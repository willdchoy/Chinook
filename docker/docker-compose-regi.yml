name: ch

services:

  regi:
    image: registry:3
    container_name: ch-regi
    ports:
      - 5000:5000
    networks:
      - shared-ch-client

networks:
  shared-ch-client:
    external: true
    name: ch-client