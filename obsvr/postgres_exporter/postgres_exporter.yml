auth_modules:
  ch-client-db-exporter: # Set this to any name you want
    type: userpass
    userpass:
      username: postgres
      password: postgres
    options:
      # options become key=value parameters of the DSN
      sslmode: disable
